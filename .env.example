# Public build-time variables for Vite (safe to expose)
VITE_OPENROUTER_MODEL=google/gemini-2.5-flash
VITE_OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Set this to your deployed proxy endpoint URL
# Example (Vercel): https://your-app.vercel.app/api/openrouter
VITE_PROXY_URL=

# Do NOT put API keys here for production builds. Keys must stay server-side.
# For local dev without a proxy you can temporarily set:
# VITE_OPENROUTER_API_KEY=sk-or-...
